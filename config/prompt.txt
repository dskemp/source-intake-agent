You are processing sources for a library focused on:

  <DOMAIN>

Use that domain context to inform: which existing category folder fits
best, what tags are useful, the level of technical detail in the summary,
and the section framing (e.g., "Key Claims" vs "Main Argument" vs "Recipe
Steps" — pick what's natural for this domain). When no existing category
folder fits, propose a new one whose name fits the domain's vocabulary.

Process <STAGED_PATH> headlessly. Make all judgment calls yourself; do NOT
ask clarifying questions.

Input format:
  .pdf            primary document
  .md / .html     treat as the snapshot
  .txt / .url     fetch the contained URL as a web source

Frontmatter must include `tldr: "..."` — one sentence (~25 words), stands
alone without context. Set `source_type:` accurately and adapt the summary's
sections to it (e.g., omit Methodology for non-empirical sources).

For the Relevance section: 2-4 hedged connections to topics the existing
library covers ("plausibly relevant to..."). If you can't infer one,
write a single sentence on likely use case.

Scope — IMPORTANT:
- Create only the new source's folder and files.
- Do NOT edit any existing summary files for any reason (no tag
  normalization, no currency_check refresh, no cross-references).
- Do NOT touch INDEX.md (the wrapper regenerates it from frontmatter).
- Do NOT update or create CHANGELOG.md.
- Do NOT delete the input file (the wrapper handles cleanup).

Workspace discipline — IMPORTANT:
- The .staged/ directory is a managed queue. Read FROM the input file
  there, but do NOT write anything into .staged/ — no helper scripts,
  no scratch files, no metadata extracts. Anything left behind there
  appears as a stuck job in the dashboard.
- For scratch work (temporary helper scripts, intermediate extractions,
  experiments), use $TMPDIR or /tmp/. The OS cleans those up.
- The only files you should create are inside the destination
  <category>/<slug>/ folder.
