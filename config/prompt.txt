Process <STAGED_PATH> headlessly via the source-intake skill.

Make all judgment calls yourself; do NOT ask clarifying questions.

Input format:
  .pdf            primary document
  .md / .html     treat as the snapshot
  .txt / .url     fetch the contained URL as a web source

Frontmatter must include `tldr: "..."` — one sentence (~25 words), stands
alone without context. Set `source_type:` accurately so the skill adapts
its sections:

  paper, report, book-chapter   scholarly — include Methodology
  article                        news, magazine, blog post — no Methodology
  opinion                        editorial / commentary — frame "Key Claims"
                                 as the author's main argument
  documentation                  technical docs / API reference

Choose an existing category folder over proposing a new one. For the
Relevance section, infer 2-4 hedged connections from the existing library
(use "plausibly relevant to..."). If you can't infer one, write a single
sentence on likely use case.

Scope — IMPORTANT:
- Create only the new source's folder and files.
- Do NOT edit any existing summary files for any reason (no tag
  normalization, no currency_check refresh, no cross-references).
- Do NOT touch INDEX.md (the wrapper regenerates it from frontmatter).
- Do NOT update or create CHANGELOG.md.
- Do NOT delete the input file (the wrapper handles cleanup).
